---
name: new-repo
description: Create a git repo for this project and push it when asked.
---
# New repo

If this folder is not a git repo, run git init, add a minimal README if none exists, and make an initial commit when the user asked for one. Push only when the user asked to push and a remote exists. Report the remote URL.
