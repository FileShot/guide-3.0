---
name: origin
description: Check the git origin remote and fix the URL the user named.
requires: args
---
# Origin

Show `git remote -v`. If the user named a new origin URL, set it and confirm with git remote -v. Do not force-push.
