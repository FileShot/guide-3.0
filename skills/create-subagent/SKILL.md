---
name: create-subagent
description: Add a focused sub-agent definition.
requires: args
---
# Create sub-agent

Write `.guide/agents/<name>.md` describing the sub-agent's job, the tools it may use, and when to stop. Keep the scope narrower than the parent agent.
