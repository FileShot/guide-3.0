---
name: create-hook
description: Add a project hook that actually runs.
requires: args
---
# Create hook

The argument says when the hook runs and what it should do. Add the hook file under the project's hook or script location, and wire it so the trigger calls it. Then show the user the path and how to fire it once.
