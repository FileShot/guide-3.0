---
name: apply-worktree
description: Apply the staged or worktree changes the user asked to take.
---
# Apply worktree

Inspect git status and the diff. Apply only the changes the user named. If they did not name a subset, show the staged diff and apply it. Report the files that changed.
