---
name: create-skill
description: Add a new skill folder with SKILL.md.
requires: args
---
# Create skill

Create `skills/<name>/SKILL.md` (or `.guide/skills/<name>/SKILL.md` in the user's project) with frontmatter `name`, `description`, and a procedure the agent can follow. The procedure must say what done looks like. Do not leave a placeholder.
