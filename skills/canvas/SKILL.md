---
name: canvas
description: Present a data-heavy answer as a structured view, not a wall of prose.
requires: args
---
# Canvas

The argument is the analysis to present. Gather the data with tools first. Then write a single HTML file in the project (or show a tight markdown table if the user said not to create files) that a person can scan: title, the numbers, and the conclusion. Do not dump raw JSON into the chat.
